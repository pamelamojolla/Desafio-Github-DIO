//git init => inicializar

//clonando repositorio => copiar a Url, criar uma pasta local na máquina

//abrir a pasta com Git Bash Here

//git clone => colar a URL clicando com o botão direito do mouse e clicando em Paste se CRTL V não funcionar

//git status => para saber se tem arquivo adicionado

// git add . => para add um novo arquivo novo no repositório

// git restore --staged => para excluir um arquivo do stage

//git commit -m "descrição que quiser" => para comitar o arquivo

//git push origin main => para enviar para o repositorio

//limpando git => CTRL L

//git log => para saber quantos commit você já fez.

//git config user.name => para saber as configurações do seu nome.

//git config user.email => para saber o seu e-mail.

//git config -p => para ter mais detalhes de log / para voltar aos comandando digitar letra Q.

//gitignore => para ignorar um arquivo dentro do seu projeto => dentro da Ide você cria um arquivo com esse nome, 
dentro dele você coloca o nome desse arquivo, depois abri o terminal da um 
git status ele vai mostrar os arquivos ignorados, depois você dá o comando de git add .gitignore e em seguida git commit -m "gitigone adicionado"
em seguida git log para verificar o log.
